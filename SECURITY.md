# 安全策略

## 支持范围

安全修复面向 `main` 分支最新代码和最新发布的容器镜像。

## 漏洞报告

请通过本仓库的 GitHub Security Advisory 私下报告漏洞，不要在公开 Issue 中披露利用细节、平台 Cookie、身份令牌、房间密码或服务器配置。报告建议包含受影响提交、复现步骤、影响判断以及已脱敏日志。

## 敏感数据

Music Together 会为账号歌单和会员音质临时处理音乐平台凭据。凭据不得出现在日志、截图、Issue、Pull Request、测试夹具或部署包中。

生产环境必须设置独立且不少于 32 字符的 `IDENTITY_SECRET`；怀疑泄露时应立即轮换，这会使已有身份 Cookie 失效。部署时还应使用最小权限账号、HTTPS/WSS，并限制数据库和备份文件的访问范围。
